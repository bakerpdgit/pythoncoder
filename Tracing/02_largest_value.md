# Finding the largest value

This example adds a decision inside the loop. `largest` changes only when the current `number` is greater than every value seen so far.

Useful trace-table columns are:

- `number`
- `largest`

The blank `largest` cells are meaningful: the `if` condition was false, so no assignment took place on that row. The value changes from `12` to `19` and then remains unchanged.
